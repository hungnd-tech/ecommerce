$kind: http-request
name: create products
description: ""
url: "{{baseUrl}}/api/products"
method: POST
body:
  type: json
  content: '{ "type": "PHYSICAL", "name": "Quan Bo Nam", "price": 299000, "stockQuantity": 1, "weightKg": 0.3 }'
auth:
  type: bearer
  credentials:
    token: "{{tokenA}}"
scripts:
  - type: afterResponse
    code: pm.environment.set("testProductId", pm.response.json().id);
    language: text/javascript
order: 3343394421328877
